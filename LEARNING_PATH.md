# 学习路线图

> 这份文档回答一个问题：**我该按什么顺序学，学到什么程度算过关。**
>
> 总时长参考：每天投入 2 小时，走完 **阶段 0 → 阶段 4** 约需 **3 个月**，
> 能打 Codeforces Div.3 和洛谷普及组；走完全程约 **8~12 个月**。

---

## 总览

| 阶段 | 名称 | 对应章节 | 预计 | 达标标志 |
|:---:|---|---|:---:|---|
| 0 | 准备期 | 01–02 | 1 周 | 能独立编译运行，会用 `vector`/`map`/`sort` |
| 1 | 入门 | 03–05 | 2 周 | 能写出二分、前缀和，能分析时间复杂度 |
| 2 | 基础数据结构 | 06–07 | 3 周 | 能默写并查集、树状数组；能用 DFS/BFS 解题 |
| 3 | 动态规划 | 08 | 3 周 | 能独立推出背包和线性 DP 的转移方程 |
| 4 | 图论 | 09 | 3 周 | 能默写最短路、最小生成树、拓扑排序 |
| 5 | 数学 | 10 | 3 周 | 理解模运算、逆元、线性基的用途 |
| 6 | 字符串 | 11 | 2 周 | 能默写 KMP 和字符串哈希 |
| 7 | 树 | 12 | 2 周 | 会 LCA、树的直径、树形 DP |
| 8 | 进阶（选学） | 13 | 长期 | 用到再查，不求全记住 |

> **不要跳阶段。** 阶段 3 的 DP 建立在阶段 2 的搜索直觉上，
> 阶段 4 的图论要用阶段 2 的并查集和阶段 3 的 DP。

---

# 阶段 0：准备期（第 1 周）

**目标**：让代码在自己电脑上跑起来，并掌握写题必需的 C++ 工具。

### 章节
- [第 01 章 环境搭建与 Hello World](docs/01-环境搭建与HelloWorld.md)
- [第 02 章 C++ 语法速成](docs/02-Cpp语法速成.md)

### 过关清单
- [ ] 能在终端看到 `g++ --version` 的版本号
- [ ] 能写出「读入两个整数，输出它们的和」并运行
- [ ] 知道 `cin/cout` 和 `scanf/printf` 的区别，知道为什么要加 `ios::sync_with_stdio(false)`
- [ ] 会用 `vector` 的 `push_back` / `size` / 遍历
- [ ] 会用 `sort` 排序数组、结构体（会写 `cmp`）
- [ ] 会用 `map` / `set` / `priority_queue` 完成「查重、计数、取最值」
- [ ] 知道 `int` 会溢出，什么时候必须用 `long long`
- [ ] 会写递归函数（会写斐波那契和汉诺塔）

### 自测题
1. 读入 n，输出 1+2+…+n。（考察循环与输入输出）
2. 读入 n 个整数，排序后输出。（考察 `vector` + `sort`）
3. 统计一段文字里每个单词出现几次。（考察 `map`）
4. 汉诺塔：读入 n，输出移动步骤。（考察递归）

**答不上来的题，说明对应章节要重看。**

---

# 阶段 1：入门（第 2 周）

**目标**：建立「算法是有复杂度代价的」这个意识，掌握最常用的三个基础算法。

### 章节
- [第 03 章 复杂度与基础算法](docs/03-复杂度与基础算法.md) —— 复杂度分析、二分、三分、gcd
- [第 04 章 前缀和·差分·双指针·位运算](docs/04-前缀和差分双指针位运算.md)
- [第 05 章 排序与贪心](docs/05-排序与贪心.md)

### 过关清单
- [ ] 能算出 `O(n)`、`O(n log n)`、`O(n²)` 在 `n = 1e5` 时大致跑多久
- [ ] 能默写「整数二分」的两种模板（求前驱 / 求后继），且**不会死循环**
- [ ] 知道什么时候用二分答案（「最大值最小」「最小值最大」→ 二分）
- [ ] 能默写一维前缀和、二维前缀和、一维差分
- [ ] 能识别滑动窗口类题目，会写双指针
- [ ] 会用位运算做「状压」和「判断奇偶/取低 bit」
- [ ] 理解贪心为什么需要证明（交换论证 / 反证）

### 自测题
1. 在有序数组中找第一个 ≥ x 的位置。（二分）
2. 给定 n 个正整数，求最小的最大段和，使得能分成 k 段。（二分答案）
3. 多次询问区间和，数组长度和询问数都是 1e5。（前缀和）
4. 给区间 [l, r] 全体加 v，共 m 次操作，最后输出整个数组。（差分）
5. 给 n 个数，求异或和最大的子集。（位运算 / 线性基预告）

---

# 阶段 2：基础数据结构（第 3~5 周）

**目标**：手写并能默写出 4 个核心数据结构。

### 章节
- [第 06 章 数据结构](docs/06-数据结构.md)
- [第 07 章 搜索与回溯](docs/07-搜索与回溯.md)

### 过关清单
- [ ] **并查集**：能默写「路径压缩 + 按秩合并」，知道它能判环、能维护连通块大小
- [ ] **树状数组**：理解 `lowbit`，能默写单点修改 + 区间查询、区间修改 + 单点查询
- [ ] **线段树**：理解「递归分区间」的思想，能默写区间求和、区间最值、懒标记
- [ ] **Trie 字典树**：知道「按字符逐层走」，能默写插入/查询
- [ ] 能区分 **DFS（栈/递归）与 BFS（队列）** 的应用场景
- [ ] 会写**回溯**（八皇后、全排列、数独），会**剪枝**
- [ ] 知道 BFS 求无权图最短路

### 自测题
1. n 个点 m 条边，判断能否构成树（无环 + 连通）。（并查集）
2. 单点加、区间求和，n 和 m 都是 1e5。（树状数组 / 线段树）
3. n 个数，m 次询问区间最大值。（线段树 / ST 表）
4. 给一堆字符串，查询某个字符串出现过几次。（Trie）
5. 迷宫最短路（4 方向）。（BFS）
6. n 皇后方案数。（回溯 + 剪枝）

---

# 阶段 3：动态规划（第 6~8 周）

**目标**：这是分水岭。**能不能自己想出状态定义，决定你能不能继续往下走。**

### 章节
- [第 08 章 动态规划](docs/08-动态规划.md)

### 过关清单
- [ ] 能说清 DP 的三要素：**状态、转移、边界**
- [ ] **01 背包**：理解为什么第二层循环要**倒序**
- [ ] **完全背包**：理解为什么第二层循环要**正序**
- [ ] 多重背包 / 分组背包 / 二维费用背包：知道怎么套
- [ ] **线性 DP**：LIS、LCS、最大子段和
- [ ] **区间 DP**：石子合并，理解「枚举断点」
- [ ] **状压 DP**：理解用二进制位表示「选了哪些」
- [ ] **树形 DP**：理解「先递归儿子，再合并到父亲」
- [ ] **数位 DP**：理解 `limit` 和 `lead` 两个标记

### 自测题
1. n 件物品，容量 W 的背包，求最大价值（每件只能取一次）。（01 背包）
2. 同上，但每件可以取无限次。（完全背包）
3. 最长上升子序列，n = 1e5。（LIS + 二分）
4. 石子合并：n 堆石子，每次合并相邻两堆，求最小总代价。（区间 DP）
5. 给一个 n×m 的棋盘，放互不攻击的棋子（n, m ≤ 10），求方案数。（状压 DP）
6. 树上选点，选了父亲就不能选儿子，求最大权值和。（树形 DP）

> **如果第 1、2 题能自己推出来**，说明你理解 DP 了；
> **如果只能背代码**，回去把「状态定义」重推十遍。

---

# 阶段 4：图论（第 9~11 周）

**目标**：把「图」当成一个能用代码操作的对象。

### 章节
- [第 09 章 图论](docs/09-图论.md)

### 过关清单
- [ ] 会用**邻接表**存图（`vector<vector<pair<int,int>>>`），知道链式前向星是什么
- [ ] **Dijkstra**：能默写堆优化版本，知道它**不能处理负权**
- [ ] **Bellman-Ford / SPFA**：知道它能处理负权、能判负环
- [ ] **Floyd**：知道 `O(n³)` 适合 `n ≤ 300`
- [ ] **最小生成树**：Kruskal（并查集 + 排序），知道边权排序后贪心是对的
- [ ] **拓扑排序**：理解「入度为 0 入队」，能判有向环
- [ ] **二分图判定**：染色法
- [ ] **欧拉路径**：知道判定条件（奇度点 0 个或 2 个）

### 自测题
1. 单源最短路，边权非负，n, m ≤ 2e5。（Dijkstra）
2. 判断图中是否有负环。（Bellman-Ford / SPFA）
3. 求最小生成树总权重。（Kruskal）
4. 给课程依赖关系，输出可行的学习顺序 / 判断有环。（拓扑排序）
5. 二分图最大匹配。（匈牙利算法）

---

# 阶段 5：数学（第 12~14 周）

**目标**：会做「取模」的题，理解数论在竞赛里的常见套路。

### 章节
- [第 10 章 数论与组合数学](docs/10-数论与组合数学.md)

### 过关清单
- [ ] **快速幂**：能默写递归和迭代两个版本
- [ ] **逆元**：理解为什么「除以 a」等于「乘 a 的逆元」（`a^(p-2)`）
- [ ] **线性筛**：能默写，理解「每个合数只被最小质因子筛一次」
- [ ] **质因数分解**：试除法 + 筛法加速
- [ ] **Miller-Rabin**：知道它判大素数（`n ≤ 1e18`）比试除法快
- [ ] **组合数**：预处理阶乘 + 逆元，`O(1)` 查询 `C(n,k)`
- [ ] **容斥原理**：会写二进制枚举版本
- [ ] **线性基**：理解「异或空间的一组基」，能求最大异或和
- [ ] **矩阵快速幂**：能把递推式（如斐波那契）转成矩阵乘法
- [ ] **exgcd / CRT**：知道用途即可

### 自测题
1. 求 `a^b mod p`。（快速幂）
2. 求 `C(n, k) mod 1e9+7`，多次询问。（阶乘 + 逆元）
3. 求 [1, n] 内所有素数。（线性筛）
4. 判断一个 `≤ 1e18` 的数是不是素数。（Miller-Rabin）
5. 求斐波那契第 n 项 mod 998244353，n ≤ 1e18。（矩阵快速幂）
6. 给 n 个数，求异或和最大的子集。（线性基）

---

# 阶段 6：字符串（第 15~16 周）

### 章节
- [第 11 章 字符串](docs/11-字符串.md)

### 过关清单
- [ ] **字符串哈希**：能默写双哈希，理解为什么能 `O(1)` 比对子串
- [ ] **KMP**：能默写 `next` 数组的求法
- [ ] **Manacher**：知道它求最长回文子串是 `O(n)`
- [ ] **Trie**：见阶段 2
- [ ] 知道 AC 自动机的用途（多模式串匹配）

### 自测题
1. 判断两个子串是否相等（多次询问）。（哈希）
2. 求字符串每个前缀的最长 border。（KMP）
3. 求最长回文子串，n ≤ 1e6。（Manacher）

---

# 阶段 7：树（第 17~18 周）

### 章节
- [第 12 章 树](docs/12-树.md)

### 过关清单
- [ ] 会求**树的直径**（两次 DFS / 树形 DP）
- [ ] 会求**树的重心**
- [ ] 能默写 **LCA**（树上倍增），理解「先跳到同一深度，再一起往上跳」
- [ ] 能写**树上差分**
- [ ] 会写**树形 DP**（见阶段 3）

### 自测题
1. 求树的直径长度。（两次 DFS）
2. 多次询问两点距离。（LCA + 深度）
3. 树上给若干路径，每条路径权 +1，最后输出每个点权值。（树上差分）

---

# 阶段 8：进阶（长期，最后再看）

到这一步你已经能看懂竞赛题了，剩下的都是「工具箱」：

- 网络流（最大流 / 最小割 / 费用流）
- 计算几何（凸包、点线关系、圆）
- 多项式（FFT / NTT、拉格朗日插值）
- 高级数据结构（主席树、莫队、树链剖分、KD-Tree）
- 博弈论（SG 函数、Nim 系列）
- 卡常技巧

→ 全部在 **[第 13 章 高级模板速查索引](docs/13-高级模板速查索引.md)**。

**不要提前学这些。** 基础不牢的时候学高级算法，只会变成背编译器。

---

# 刷题平台推荐

| 平台 | 适合 | 说明 |
|---|---|---|
| [洛谷](https://www.luogu.com.cn/) | 中文，从入门到省选 | 有「题单」功能，按知识点分类 |
| [Codeforces](https://codeforces.com/) | 英文，比赛多 | 从 **Div.3 / Div.4** 开始打 |
| [AtCoder](https://atcoder.jp/) | 英文/日文 | **ABC** 的 A~D 题非常适合练手 |
| [牛客](https://ac.nowcoder.com/) | 中文，有周赛 | 题解质量好 |

---

# 常见的三个坑

**坑 1：只看不写。**
看懂 ≠ 会写。每道题必须自己敲一遍，敲不出来再看答案。

**坑 2：跳阶段。**
DP 没学透就去搞网络流，最后什么都记不住。按顺序来。

**坑 3：不总结。**
每做完一道题，用一句话写下「这题的关键点是什么」。
**你犯错的地方，就是你下次还会犯错的地方。**

---

# 怎么用这个仓库

1. 打开对应章节的 `docs/xx-xxx.md`
2. 读完「概念」和「代码」，**先不看代码自己敲一遍**
3. 做章节末尾的练习
4. 做不出来 → 回看「常见坑」那一节
5. 还想不通 → 看 `solutions/` 里有没有同类题的题解

祝你早日 AK 一场比赛。
